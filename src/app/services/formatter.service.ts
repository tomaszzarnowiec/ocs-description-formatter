import { Injectable } from '@angular/core';

@Injectable()
export class FormatterService {
  constructor() {}

  formatDescription(config: any): string {
    let description = '<div class="ocs-desc">';

    description = '</div>';
    return description;
  }
}
